
object Construct

contract:
  manifest


OutVarHelper task given out
  set vars empty
  if out
    ply out
      push'd vars it.0.1
  return vars

ExprsHelper task given exprs
  return ?exprs.2 ? empty
